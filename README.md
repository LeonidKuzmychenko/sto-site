# STO Site — Frontend

Лендинг для СТО, реализованный на **React + Vite + TypeScript**.
Проект ориентирован на чистую архитектуру, масштабируемость и коммерческое использование.

Используется:
- **React 19**
- **Vite**
- **TypeScript**
- **Tailwind CSS**
- **i18next / react-i18next** (UA / EN / RU)

---

## 🗂 Актуальная структура проекта

```
src/
│
├── app/
│   ├── App.tsx            # корневая композиция приложения
│   └── main.tsx           # точка входа (bootstrap)
│
├── pages/
│   └── HomePage.tsx       # главная страница лендинга
│
├── widgets/
│   ├── header/
│   │   └── Header.tsx     # фиксированный header
│   ├── footer/
│   │   └── Footer.tsx     # footer страницы
│   └── sections/
│       └── Sections.tsx   # список секций страницы
│
├── features/
│   └── nav/
│       └── Nav.tsx        # навигация + переключатель языка
│
├── entities/
│   └── section/
│       ├── model/
│       │   ├── types.ts              # типы секций
│       │   ├── sections.config.ts    # единый конфиг секций (source of truth)
│       │   ├── selectors.ts          # селекторы (nav, порядок и т.д.)
│       │   └── useActiveSection.ts   # scroll-spy (IntersectionObserver)
│       │
│       └── ui/
│           ├── BaseSection.tsx
│           ├── ServicesSection.tsx
│           ├── PricesSection.tsx
│           ├── ContactsSection.tsx
│           └── SectionRenderer.tsx
│
├── shared/
│   ├── providers/
│   │   ├── index.ts       # инициализация i18n
│   │   └── locales/
│   │       ├── ua.json    # украинский (default)
│   │       ├── en.json    # английский
│   │       └── ru.json    # русский
│   │
│   └── styles/
│       └── index.css      # глобальные стили и CSS-переменные
│
└── vite-env.d.ts
```

---

## 🧠 Архитектурные принципы

Проект следует упрощённому **Feature-Sliced Design**:

- **app/** — bootstrap и композиция
- **pages/** — страницы
- **widgets/** — крупные UI-блоки страницы
- **features/** — пользовательские сценарии (Nav)
- **entities/** — доменные сущности (Section)
- **shared/** — общие ресурсы (i18n, стили)

---

## 🧩 Секции и навигация

- `sections.config.ts` — **единый источник правды**
- Из конфига автоматически формируются:
    - секции страницы
    - пункты навигации
    - порядок меню
- Навигация не содержит хардкода

Добавление новой секции = **одна правка в конфиге**.

---

## 🎯 Active navigation (Scroll‑spy)

- Реализовано через **IntersectionObserver**
- Без сторонних библиотек
- Учитывает высоту фиксированного header
- Активный пункт меню подсвечивается при скролле

Логика изолирована в `useActiveSection`.

---

## 🌍 Мультиязычность

Поддерживаемые языки:
- **UA** — язык по умолчанию
- **EN**
- **RU**

Особенности:
- выпадающий список переключения языка
- i18n и локали вынесены в `shared/providers`
- готово к сохранению языка и SSR

---

## 🎨 Tailwind CSS

Используется для всей стилизации проекта.

Основные моменты:
- минималистичный UI
- CSS‑переменные (`--header-height`)
- отсутствие inline‑стилей

---

## ⚙️ Примечания

- `App.tsx` не содержит бизнес-логики
- Header / Footer не зависят от страниц
- Nav не знает о структуре секций
- Архитектура готова к:
    - SSR (Next.js)
    - мобильному меню
    - расширению языков
    - A/B тестам

---

## 📌 Статус

Проект находится в активной разработке.
Архитектура стабилизирована и готова к дальнейшему расширению.

