# STO Site — Frontend

Коммерческий frontend‑проект для сайта СТО.
Реализован как **многостраничное SPA** на **React + Vite + TypeScript** с клиентским роутингом, выпадающим меню и мультиязычностью.

Проект ориентирован на:

* чистую архитектуру
* масштабируемость
* SEO‑дружественную структуру URL
* дальнейшее развитие (SSR / mobile / новые услуги)

---

## 🧱 Технологический стек

Используется:

* **React 19**
* **Vite**
* **TypeScript**
* **React Router DOM**
* **Tailwind CSS**
* **i18next / react-i18next** (UA / RU)
* **lucide-react** (иконки)

---

## 🗂 Актуальная структура проекта

```
src/
│
├── app/
│   ├── App.tsx            # подключение RouterProvider
│   ├── main.tsx           # bootstrap приложения
│   └── router.tsx         # конфигурация маршрутов
│
├── pages/
│   ├── Layout.tsx         # layout (Header + Footer + Outlet)
│   ├── HomePage.tsx       # главная страница
│   ├── PricesPage.tsx     # цены
│   ├── ContactsPage.tsx   # контакты
│   ├── NotFoundPage.tsx   # 404
│   │
│   └── services/
│       ├── ServicesPage.tsx    # контейнер услуг (Outlet)
│       ├── StartersPage.tsx    # ремонт стартеров
│       └── GeneratorsPage.tsx  # ремонт генераторов
│
├── features/
│   └── nav/
│       └── Nav.tsx        # основная навигация + dropdown + язык
│
├── widgets/
│   ├── header/
│   │   └── Header.tsx     # обёртка header
│   └── footer/
│       └── Footer.tsx     # footer (sticky)
│
├── shared/
│   ├── providers/
│   │   ├── index.ts       # инициализация i18n
│   │   └── locales/
│   │       ├── ua.json    # украинский (default)
│   │       └── ru.json    # русский
│   │
│   └── styles/
│       └── index.css      # глобальные стили
│
└── vite-env.d.ts
```

---

## 🧠 Архитектура

Проект использует **страничную модель**, а не лендинг с секциями.

### Принципы:

* **1 URL = 1 Page**
* навигация через **React Router**
* общий layout через `<Outlet />`
* без scroll‑spy и «активных секций»
* header и footer не зависят от страниц

### Роли слоёв:

* **app/** — bootstrap, router
* **pages/** — страницы (route‑level)
* **features/** — пользовательская логика (Nav)
* **widgets/** — крупные UI‑блоки
* **shared/** — i18n, стили, общие ресурсы

---

## 🧭 Навигация

* Основная навигация реализована в `features/nav/Nav.tsx`
* Пункт **«Послуги»** содержит:

    * выпадающее меню
    * стрелку‑индикатор (`ChevronDown`)
    * анимацию поворота при hover и active‑состоянии
* Активные пункты и подпункты:

    * корректно подсвечиваются
    * родитель активен при `/services/*`

Навигация **не дублируется** на страницах.

---

## 🛣 Роутинг

Используется `createBrowserRouter` с `basename`.

Поддерживаемые маршруты:

* `/` — главная
* `/services/starters`
* `/services/generators`
* `/prices`
* `/contacts`
* `*` → `NotFoundPage`

Маршрут `/services` автоматически редиректит на `/services/starters`.

---

## 🌍 Мультиязычность

Поддерживаемые языки:

* **UA** — основной (default)
* **RU** — дополнительный

Особенности:

* переключатель языка в header
* язык не привязан к URL
* готово к сохранению в `localStorage`
* i18n полностью изолирован в `shared/providers`

---

## 🎨 Tailwind CSS

* используется для всей стилизации
* layout на flex
* footer «прижат» к низу страницы без `position: fixed`
* underline реализован через pseudo‑element (`::after`)
* без inline‑стилей

---

## ⚙️ Конфигурация

* используется alias `@ → src`
* настроен в:

    * `vite.config.ts`
    * `tsconfig.app.json`
* поддерживается деплой в подкаталог (`/sto-site/`)

---

## 📌 Статус проекта

* архитектура стабилизирована
* роутинг и навигация завершены
* готов к:

    * добавлению новых услуг
    * mobile‑меню
    * SEO‑оптимизации
    * SSR / prerender при необходимости

---

## 🔜 Возможные следующие шаги

* mobile‑burger с тем же dropdown
* динамические услуги (`/services/:slug`)
* SEO‑meta на каждый роут
* сохранение языка
* sitemap.xml
