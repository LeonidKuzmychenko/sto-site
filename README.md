# STO Site — Frontend

Проект лендинга на **React + Vite + TypeScript**  
Используется **Tailwind CSS** для стилей и **i18next** для мультиязычности.  
Архитектура ориентирована на масштабирование и аккуратную поддержку.

---

## 🎨 Добавление Tailwind CSS

### Установка зависимостей
```bash
npm install -D tailwindcss@3.4.17 postcss autoprefixer
```

### Инициализация Tailwind
```bash
npx tailwindcss init -p
```

Будут созданы файлы:
```
tailwind.config.js
postcss.config.js
```

---

## 🌍 Добавление i18n

### Установка
```bash
npm install i18next react-i18next
```

i18n используется для поддержки нескольких языков (RU / EN).  
Конфигурация и словари вынесены в отдельный модуль.

---

## 🗂 Структура проекта

```
src/
│
├── app/
│   ├── App.tsx
│   ├── main.tsx
│   └── i18n/
│       ├── i18n.ts
│       └── locales/
│           ├── en.json
│           └── ru.json
│
├── components/
│   ├── layout/
│   │   ├── Header.tsx
│   │   ├── Footer.tsx
│   │   └── Layout.tsx
│   │
│   └── sections/
│       └── Sections.tsx
│
├── features/
│   └── nav/
│       └── Nav.tsx
│
├── shared/
│   └── styles/
│       └── index.css
│
└── vite-env.d.ts
```

---

## 🧠 Архитектура

- **`app/`** — точка входа приложения  
  Аналог подхода Next.js / modern React

- **`layout/`** — переиспользуемый каркас страницы  
  Header, Footer, Layout

- **`features/`** — бизнес-логика  
  `Nav` — это feature, а не layout-компонент

- **`shared/`** — общие стили, утилиты, UI

---

## ⚙️ Примечания

- `App.tsx` отвечает только за композицию
- Layout изолирует структуру страницы
- Проект готов к подключению роутинга и SSR
- `vite-env.d.ts` обязателен для корректной работы TypeScript + Vite

---
